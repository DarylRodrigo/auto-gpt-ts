// import { GenerateBashCommand } from "../src/commands";
// // import { createDirectory, writeToFile } from "../src/commands/FileCommands";

// createDirectory.execute({ args: ["test"] });
// writeToFile.execute({ args: ["test awefa wef awef", "test.txt"]})

// const execute_code = ExecuteBashCommand.create();
// execute_code.execute({ code: "ls -la" });
